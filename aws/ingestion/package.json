{
  "name": "written-semantic-ingestion",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Authenticated ingestion endpoint for the v0.3.1 encrypted raw vault.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "./build.sh"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@aws-sdk/client-kms": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@aws-sdk/client-lambda": "^3.700.0"
  }
}
